<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hjl.module_main">

    <application>
        <activity android:name=".mvp.activity.MusicListActivity"></activity>
        <activity android:name=".mvp.activity.MusicDetailActivity" />
        <activity android:name=".mvp.activity.UserSettingActivity" />
        <activity android:name=".mvp.activity.MainActivity"
            android:theme="@style/MusicAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".mvp.fragment.MusicService"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>